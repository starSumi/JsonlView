[package]
name = "jsonl-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "UNLICENSED"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
memchr = "2.8.3"
napi = { version = "3.12.2", default-features = false, features = ["napi8"] }
napi-derive = "3.6.3"

[build-dependencies]
napi-build = "2.4.1"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
